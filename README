Code for a multi-jointed walking toy robot driven by an arduino.

arduino-mk is needed for compilation; the Arduino IDE will not work.

This project makes heavy use of other projects via submodules.  You'll need to
`git submodule init` to make it work.
